apiVersion: v1
kind: Secret
metadata:
  name: apex-financial-secrets
  namespace: default
type: Opaque
stringData:
  # Replace with actual values — never commit real secrets
  DATABASE_URL: "postgresql://postgres:CHANGE_ME@postgres-service:5432/apex_financial"
  FIELD_ENCRYPTION_KEY: "CHANGE_ME_base64_encoded_32_byte_key"
